/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
int r;
r = _isalpha(r + 'H');
_putchar(r + '0');
r = _isalpha(r + 'o');
_putchar(r + '0');
r = _isalpha(108);
_putchar(r + '0');
r = _isalpha(r + ';');
_putchar(r + '0');
_putchar('\n');
return (0);
}

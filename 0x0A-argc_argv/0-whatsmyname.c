#include "main.h"
#include <stdio.h>
/**
 *main - is our main function
 *@argc: is the argument count passed in the command line
 *@argv: is an array of arguments passed in the command line
 *Return: 0 (Success)
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}

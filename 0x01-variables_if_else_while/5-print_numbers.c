#include <stdio.h>
/**
 * main - is the starting point of the program
 * Return: 0 (Success)
*/
int main(void)
{
int nums;
for (nums = 0; nums < 9; nums++)
{
printf("%d", nums);
}
printf("%d\n", nums);
return (0);
}
